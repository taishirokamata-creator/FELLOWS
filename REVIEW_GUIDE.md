# FELLOWS Wiki 世界観レビュー方針（共有用）

両PC・両セッション（＋しのぴ）で足並みを揃えるための編集ガイド。
内容を書くとき・レビューするときは、まずこれを読む。技術的な構成は `HANDOFF.md` を参照。

最終更新：2026-08-02

---

## 0. いちばん大事なこと
- **主役はFELLOWS独自の世界観**。しのぴ達がみんなで作り上げてきた設定・経緯・小ネタを厚くする。
- **外部/一般情報（バンド・お店・元ネタのwikipedia等）は薄く**。参考リンク＋1〜2行に留める。書きすぎない。
- **口調は淡々と・事実ベース**（2026-08-02方針転換。以前の“ちょっとふざけた口調”はやめる）。盛らない／勝手に解釈・断定しない。短く素直に。
  - ❌ クリシェ・決めつけ：「人狼なのに“結束で終わる”」「FELLOWSらしい温度感」「初回から全開だった」
  - ❌ 誇張・装飾：「まるっと」「伝説の」／確証のない推測の断定
  - ⭕ あらすじ・設定・元ネタ・確認できた小ネタを淡々と。不確かは「？」で残す。
  - **鎌田が『』で括った表現だけ**、その言い回しをそのまま採用する（それ以外は淡々と自分の言葉で）。
- **造語禁止**：FELLOWSで実際に使われている単語・呼称だけを使う。AIが説明用に“それっぽい見出し語／用語”を発明しない（例：「改名メタ」はNG＝AI造語だった）。名前が要る時は造語ではなく**素直な説明的表現**にする。由来が不確かな語は本人に確認。既存の見出しも造語がないか棚卸しする。
  - ※旧口調で書いた項目（ハロウィン/NEVER/学園Z/人狼ルーム/エバヤン/ぴろうず/天下一 等）は淡々に直す対象。

## 1. 不確かな情報の扱い
- しのぴ自身も「覚えてない」部分が多い。**確定していないことは消さず、「？」を付けて残す**。
  - 例：「ブルー将軍（2回ほど登場した？）」「第5・7・8回のSGは不明・要確認」
- 後でしのぴが読んで直せるように、**曖昧さを正直に見せる**のが方針。
- **「調べるリスト（open_questions）」は運用停止**（2026-08-02〜）。不確か・要確認は下記の**修正リスト元ファイル＋スプレッドシート**へ集約する（Wiki本文には「？」付きで残す）。

## 2. データの正と反映フロー
- **正データは `年表/data/fellows_db.json` の1つだけ**。wiki.html / index.html / Obsidian金庫は**全部そこからの生成物**。
  → HTMLやObsidianを直接編集しない（次のビルドで消える）。直すのは必ずJSON。
- 反映手順：
  1. `fellows_db.json` を編集（events / chars / world / keywords / canon）
  2. `python build/build_all.py`（金庫・wiki・index・Artifactソースを一括再生成）
  3. `年表/` で `git add -A && git commit -m "..."`
  4. **push は区切りのいいところでまとめて**（コメント毎に毎回pushしない）。Artifactは都度Publishでプレビュー可。

## 3. 一次情報の使い方
- **しのぴ面談の文字起こし（Notion）× 既存Obsidian** を突き合わせて厚くする。
- 文字起こしは音声認識の誤変換が多い（例：「展開地ブローカー」＝「天下一武狼会」の誤変換）。**鵜呑みにせず解釈**する。
- 修正・追加の受け皿＝**修正リスト元ファイル `FELLOWS/FELLOWS_Wiki修正リスト元.tsv`（ドライブ同期・非公開・git対象外）** ＋ **修正管理スプレッドシート**（下記）。
  - 追記は元ファイル(TSV)に自由に足す。**スプレッドシートはAIが上書き生成しない**（本人が直接編集するため）。新規行はTSVから**手動コピペ**でシートに足す。
  - しのぴ／各自が起票→反映→状態を「対応済」に。

## 4. 並行編集の注意（重要）
- この `.git` は **Google Drive経由で複数PCと共有**されている。同時に別PCが編集・pushしていることがある。
- **編集はなるべく片方のPCに寄せる**。やむを得ず両方でやるときは：
  - commit前に `git fetch` → 相手の変更を取り込んでから作業
  - push が弾かれたら（non-fast-forward）＝相手が先にpush済み。`git pull`（or fetch＋merge）してから push
  - 生成物（wiki.html等）はJSONから再生成すれば一致するので、コンフリクトは基本JSON側で解決

## 5. 用語・構成の決めごと
- カテゴリ名：**年表／公演／キャラクター／用語集**（「人物」→「キャラクター」、「設定＋キーワード」→「用語集」に統一済み）。
- Wiki左目次：五十音の行見出し（ア行/カ行…）は**アコーディオン**（押すと展開）。漢字始まりは `build_wiki.py` の `YOMI` に読みを足すと正しい行に入る。
- 年表：**作中年表**（FELLOWS世界の人狼の作中年代・線表）＋**現実の開催史**（開催日/公演/内容）。「正史／2076年が現在」等の正典注記は使わない。
- 表記統一：「FELLOWS学園」（カタカナ「フェローズ」にしない）。公演の公式タイトル（フェロウズ、月へ 等）はそのまま。
- **登場人物の表示（公演ページ）**：その回で使われた表記（as-played）で出す。キャラを演じた回は **`キャラ名（中の人：本人）`** 形式で書く（例：シノの魂＝`悪いAI／AI（中の人：しの）`）。リンクは正規キャラページへ別名リンク（`[[正規名|表示名]]`）。**本人名義で出た人はキャラクター一覧に別出ししない**。本人かキャラかは**個別に指示して都度補正**（データに印が無いので自動判定しない）。

## 6. リンク・参考先
- Wiki（公開）: https://taishirokamata-creator.github.io/FELLOWS/wiki.html
- 年表（公開）: https://taishirokamata-creator.github.io/FELLOWS/
- 修正管理スプレッドシート: https://docs.google.com/spreadsheets/d/1DGg8j4EB8VV7OurY2OIEX_a-QGok_0E_TahCO6zksKw/edit
- 修正リスト元ファイル（非公開・ドライブ同期）: `FELLOWS/FELLOWS_Wiki修正リスト元.tsv`
- 技術構成・移行手順: `年表/HANDOFF.md`

## 7. 決定ログ（2026-08-02）
- **あらすじの粒度**：導入→中盤の転換点→クライマックス／オチ→主要キャラ→小ネタ→未確定、をNotion追記テンプレに沿って厚く（鎌田のノリ・？付き）。既に反映：シノの魂／ストーリー人狼9月編。
- **FELLOWS貸切の初回**：ストーリー人狼の第1回＝9月編(2023.9.19)。正史の「2021年12月＝FELLOWS貸切誕生」は“場（貸切）の源流”として**そのまま残す**（別物として共存＝A採用）。
- **保留中**：シノの魂で「悪いAI」がキャラの自動スタブとして生成され、用語「悪いAI（宇宙外生命体）」と併存中（あとで整理）。
